export interface initialUserType {
    users: any[]
    loadingUsers: boolean
}


export interface initialTodoType {
    todos: any[]
    loading: boolean
    completed?: boolean
    checking?: boolean
    deleting?: boolean
}